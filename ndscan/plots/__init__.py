"""PyQtGraph plotting code for ndscan.

The package is now split conceptually into:

- ``ndscan.plots.legacy`` for the historical flat-dataset runtime
- ``ndscan.plots.runtime`` for the prepared-runtime site-tree viewer

The existing top-level modules remain in place for now because the legacy plot stack
still physically lives here.
"""
