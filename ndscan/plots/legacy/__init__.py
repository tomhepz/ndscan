"""Legacy ndscan plotting stack.

This package names the pre-prepared-runtime plotter explicitly as legacy. The existing
code still lives in the parent ``ndscan.plots`` modules for now, but new code should
prefer importing the legacy path explicitly when it depends on the old dataset model.
"""

